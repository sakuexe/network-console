---
import Layout from '../../../layouts/Layout.astro'
import DeviceInfo from './deviceinfo'
import './remove.css'

const isLoggedIn = Astro.cookies.has('loggedIn')
---

<Layout title={`Remove device?`}>
  <DeviceInfo client:idle deviceId={Astro.params.id || 0} isLoggedIn={isLoggedIn}/>
</Layout>
